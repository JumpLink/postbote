{
  "name": "@postbote/whatsapp",
  "version": "0.1.0",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "type": "module",
  "description": "WhatsApp backend: the delivery sync driver over Baileys (unofficial multi-device protocol: WebSocket, WASM, libsignal), its auth state on postbote's SecretStore, and the QR / pairing-code login. Read-only. Self-contained: no other postbote package imports it.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc --noEmit --project tsconfig.json"
  },
  "files": [
    "src"
  ],
  "dependencies": {
    "@postbote/protocol": "*",
    "@postbote/store": "*",
    "baileys": "7.0.0-rc14",
    "qrcode-generator": "2.0.4",
    "ws": "8.21.3"
  },
  "devDependencies": {
    "@types/ws": "8.18.1"
  }
}
