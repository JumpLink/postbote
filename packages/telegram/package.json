{
  "name": "@postbote/telegram",
  "version": "0.1.0",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "type": "module",
  "description": "Telegram backend: the chat sync driver over mtcute (pure-TS MTProto on web standards), a session store on postbote's own SQLite, and the interactive login. Read-only.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc --noEmit --project tsconfig.json"
  },
  "files": [
    "src"
  ],
  "dependencies": {
    "@mtcute/core": "0.32.3",
    "@mtcute/web": "0.32.3",
    "@postbote/protocol": "*",
    "@postbote/store": "*"
  }
}
